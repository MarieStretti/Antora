'use strict'

function loremBlockMacro () {
  this.process((parent, target, attrs) =>
    this.createBlock(parent, 'paragraph', ['Lorem ipsum'])
  )
}

function register (registry) {
  registry.blockMacro('lorem', loremBlockMacro)
}

module.exports = loremBlockMacro
module.exports.register = register
