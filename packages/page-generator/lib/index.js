'use strict'

/**
 * Page Generator component for Antora
 *
 * Wraps the embeddable HTML contents stored in pages in the content catalog in
 * a standalone page layout.
 *
 * @namespace page-generator
 */
module.exports = require('./create-page-generator')
