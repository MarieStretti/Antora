language: node_js
node_js:
  - 12
before_script:
  - npm install -g gulp-cli
script: gulp
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s --
  - export PATH="$HOME/.yarn/bin:$PATH"
